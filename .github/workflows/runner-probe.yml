name: Runner Probe

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/runner-probe.yml'
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo RUNNER_OK
